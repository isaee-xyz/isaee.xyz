# Copy to .env.local and fill in.
# WARNING: this key is embedded in the client bundle (static site — it is public).
# Use a referrer-restricted, quota-capped Gemini key, or leave empty for demo mode.
VITE_GEMINI_API_KEY=
